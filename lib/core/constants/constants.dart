import 'package:flutter/material.dart';

class Constants {
  static String currency = "QAR";
  static String appName = "Mawater Qatar";

  static Locale engLocal = const Locale('en');
  static Locale arLocal = const Locale('ar');
}
